#!/bin/bash
devNode=$1
mkfs.ext4 /dev/${devNode}5
#mkfs.ext4 /dev/${devNode}2
mkfs.ext4 /dev/${devNode}7
e2label /dev/${devNode}5 bp_boot
e2label /dev/${devNode}7 bp_root
mkswap -L bp_swap /dev/${devNode}6
swapon /dev/${devNode}6

